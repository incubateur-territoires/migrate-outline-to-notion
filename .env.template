# Notion Configuration
# ------------------
# Your Notion API key from https://www.notion.so/my-integrations
NOTION_API_KEY=[your-notion-api-key]
# The ID of the Notion page where content will be migrated to
NOTION_DESTINATION_PAGE_ID=[your-destination-page-id]

# Outline Configuration
# -------------------
# Path to your Outline export directory containing markdown files
OUTLINE_EXPORT_PATH=[path-to-your-outline-export]

# File Storage Strategy
# -------------------
# Set to 'true' to keep original Outline file URLs instead of uploading to a new bucket
USE_ORIGINAL_BUCKET=false
# Required only if USE_ORIGINAL_BUCKET=true - The base URL of your original Outline bucket
# Example: https://your-outline-bucket.s3.region.amazonaws.com
ORIGINAL_BUCKET_END_POINT=[your-original-bucket-endpoint]

# AWS/S3 Configuration
# ------------------
# Required ONLY if USE_ORIGINAL_BUCKET=false
# Your S3-compatible storage configuration for uploading files to a new bucket
AWS_REGION=[your-s3-region]
AWS_S3_BUCKET=[your-s3-bucket-name]
AWS_ACCESS_KEY_ID=[your-s3-access-key-id]
AWS_SECRET_ACCESS_KEY=[your-s3-secret-access-key]
# Optional: Custom endpoint URL if not using AWS S3 (e.g., for Scaleway)
AWS_ENDPOINT=[your-s3-endpoint-if-not-using-aws]